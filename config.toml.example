#gorm
[log]
level = "info"

#App configuration values
[base]
addressRegex = "osmo(valoper)?1[a-z0-9]{38}"
addressPrefix = "osmo"
startblock = 4823317 #4740820 
endblock = -1 # no end block
throttling = 0
blockTimer = 10000 #print out how long it takes to process this many blocks
waitForChain = false #if true, indexer will start when the node is caught up to the blockchain
waitForChainDelay = 10 #seconds to wait between each check for node to catch up to the chain
indexingEnabled = true #If false, TX won't be added to the DB but other values might be READ from the DB
exitWhenCaughtUp = true #mainly used for Osmosis rewards indexing
osmosisRewardsOnly = false #only index Osmosis rewards, do not start other threads or processes.

#Lens config options
[lens]
homepath = "/home/kyle/.lens"
rpc = "http://52.35.17.25:26657" #"http://45.56.94.181:26657"
key = "default"
accountPrefix = "osmo"
keyringBackend = "test"
chainID = "osmosis-1"
chainName = "Osmosis"

#postgresql
[database]
host = "localhost"
port = "5432"
database = "taxappdb"
user = "taxapp"
password = "taxapptest"